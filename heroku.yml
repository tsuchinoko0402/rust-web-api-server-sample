setup:
  addons:
  - plan: heroku-postgresql:hobby-dev
    as: DATABASE
build:
  docker:
    web:
      dockerfile: ./server/Dockerfile
      target: production
    migration:
      dockerfile: ./server/Dockerfile
      target: build-stage
release:
  image: migration
  command:
    - diesel setup
